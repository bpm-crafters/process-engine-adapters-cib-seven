<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>dev.bpm-crafters.process-engine-adapters</groupId>
    <artifactId>process-engine-adapter-cib-seven-root</artifactId>
    <version>2026.08.2-SNAPSHOT</version>
    <relativePath>../../pom.xml</relativePath>
  </parent>

  <artifactId>process-engine-adapter-cib-seven-remote-core</artifactId>
  <name>Adapter: CIB7 Remote Core</name>
  <description>
    CIB seven Remote Adapter core. Talks to a standalone CIB seven engine over its REST API,
    using the community-maintained REST client
    (org.cibseven.community.rest-client:cibseven-engine-rest-client-openapi-java).
  </description>

  <properties>
    <cibseven-rest-client.version>2.2.0</cibseven-rest-client.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <!-- https://central.sonatype.com/artifact/org.cibseven.bpm/cibseven-bom -->
      <dependency>
        <groupId>org.cibseven.bpm</groupId>
        <artifactId>cibseven-bom</artifactId>
        <version>${cibseven.version}</version>
        <scope>import</scope>
        <type>pom</type>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>dev.bpm-crafters.process-engine-api</groupId>
      <artifactId>process-engine-api</artifactId>
      <version>${process-engine-api.version}</version>
    </dependency>
    <dependency>
      <groupId>dev.bpm-crafters.process-engine-api</groupId>
      <artifactId>process-engine-api-impl</artifactId>
      <version>${process-engine-api.version}</version>
    </dependency>

    <!-- Subscription-based service task delivery using the official CIB seven external task client -->
    <dependency>
      <groupId>org.cibseven.bpm</groupId>
      <artifactId>cibseven-external-task-client</artifactId>
      <optional>true</optional>
    </dependency>

    <!-- Typed-value API (VariableMap, TypedValue, ValueType, …) used by the ported value mapper -->
    <dependency>
      <groupId>org.cibseven.commons</groupId>
      <artifactId>cibseven-commons-typed-values</artifactId>
      <scope>provided</scope>
    </dependency>

    <!-- Spin: (de)serialization of typed process variables (JSON/XML/object) over REST -->
    <dependency>
      <groupId>org.cibseven.bpm</groupId>
      <artifactId>cibseven-engine-plugin-spin</artifactId>
      <scope>provided</scope>
    </dependency>

    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-web</artifactId>
      <scope>provided</scope>
    </dependency>

    <!-- Community-maintained CIB seven REST client (replaces the in-house generated client) -->
    <dependency>
      <groupId>org.cibseven.community.rest-client</groupId>
      <artifactId>cibseven-engine-rest-client-openapi-java</artifactId>
      <version>${cibseven-rest-client.version}</version>
    </dependency>

    <!-- Directly used by DeploymentApiImpl: multipart request (httpclient5) and the
         ApiClient's ObjectMapper to read the deployment response (jackson) -->
    <dependency>
      <groupId>org.apache.httpcomponents.client5</groupId>
      <artifactId>httpclient5</artifactId>
    </dependency>
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-core</artifactId>
    </dependency>
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
    </dependency>

    <!-- Directly used by the Spin value serializers (@PostConstruct) -->
    <dependency>
      <groupId>jakarta.annotation</groupId>
      <artifactId>jakarta.annotation-api</artifactId>
    </dependency>

    <!-- For JGiven Testing Stage as a part of compiled code (mirrors embedded-core) -->
    <dependency>
      <groupId>com.tngtech.jgiven</groupId>
      <artifactId>jgiven-core</artifactId>
      <version>${jgiven.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.awaitility</groupId>
      <artifactId>awaitility-kotlin</artifactId>
      <version>${awaitility.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <scope>provided</scope>
    </dependency>

    <!-- Testing -->
    <dependency>
      <groupId>dev.bpm-crafters.process-engine-adapters</groupId>
      <artifactId>process-engine-adapter-cib-seven-testing</artifactId>
      <version>${project.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

</project>
